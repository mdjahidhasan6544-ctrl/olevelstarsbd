# Production backend URL (set to your deployed backend service)
VITE_API_URL=https://<backend-url>

# Optional: override when testing GitHub Pages-style routing locally.
# In GitHub Actions deploy workflow this is set automatically to /<repo-name>/.
VITE_BASE_PATH=/

# Local development example:
# VITE_API_URL=http://localhost:3001
